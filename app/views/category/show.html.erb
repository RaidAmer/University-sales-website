<h1><%= @category.name %></h1>
<p><%= @category.description %></p>

<% if @category.icon.attached? %>
  <%= image_tag @category.icon %>
<% end %>

<p><strong>Featured:</strong> <%= @category.is_featured ? 'Yes' : 'No' %></p>

<%= link_to "Back to Categories", categories_path, class: "btn btn-secondary mt-3" %>